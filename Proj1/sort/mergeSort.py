# Merge Sort
# --------------
# Input: Unsorted array
# Output: Sorted array
# Description: Sorts an array using merge sort. Worst case O(n lg n)
import copy

class MergeSort:
    def __init__(self, array) -> None:
        self.name = "Merge Sort"
        self.array = copy.deepcopy(array)
    
    def merge(self, start, end) -> None:
        return 1
    
    def sort(self) -> None:
        return 1
